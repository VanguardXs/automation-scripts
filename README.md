# Automation Scripts

Python automation and utility scripts for everyday tasks and workflows.

This repository contains small, focused tools designed to automate common operations with files and data.

## Structure

- `data_utils/` — scripts for working with structured data (CSV, Excel, etc.)
- `file_utils/` — scripts for file system automation and batch operations

Each folder contains its own README with usage details.
